DATABASE_URL=postgres://aici:aici@localhost:5432/aici

# Staff login. Make each secret with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# The database is shared, so STAFF_PASSWORD_PEPPER and MFA_ENCRYPTION_KEY must be
# the same for the whole team. Get them from a teammate, never commit them.
STAFF_PASSWORD_PEPPER=
MFA_ENCRYPTION_KEY=
ALTCHA_HMAC_KEY=
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3000
